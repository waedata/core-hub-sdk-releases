# Core Hub SDK Releases

Private, immutable, versioned GitHub Release channel for Core Hub client authorization SDK artifacts.

- Source and release workflow authority: `wae2855/core-hub`
- This repository stores release tags and assets only; it is not a source-code authority.
- Consumers must use a versioned release URL and verify the frozen SHA-256 manifest.
- Draft assembly and publication are separate protected operations.
